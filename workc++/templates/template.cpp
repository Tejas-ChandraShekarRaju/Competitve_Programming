#include<bits/stdc++.h>

using namespace std;

#define fr(i,n) for(int i=0;i<n;i++)

template<typename... T>
void read(T&... args)
{
    ((cin>>args), ...);

}

template<typename... T>
void write(T... args)
{
    ((cout<<args<<" "), ...);
}

int main()
{

 vector<int> arr(10,0);

}